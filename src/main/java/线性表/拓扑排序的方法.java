package 线性表;

import org.junit.Test;

/**
 * 题目：
 * 拓扑排序：对一个有向无环图(Directed Acyclic Graph， DAG)G进行拓扑排序，是将G中所有顶点排 成线性序列，使得图中任意一对顶点u、v， 若边(u,v)∈E(G)，则在线性序列中u出现在v 之前。
 */
public class 拓扑排序的方法 {

    @Test
    public void test(){
        
    }
}